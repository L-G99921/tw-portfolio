{
  "openapi": "3.0.3",
  "info": {
    "title": "PowerBox API",
    "description": "The **PowerBox API** is the integration surface between a utility's back-office systems and the EnergyGrid platform. It carries the two flows a utility partner needs to wire up before customers can use any EnergyGrid-powered hardware or app:\n\n- **Customer synchronisation** — push customer accounts, service addresses (Premises), and meter configurations from the utility Customer Information System (CIS) into EnergyGrid.\n- **Hardware binding** — link a DataBridge or SmartLink device to its target Advanced Metering Infrastructure (AMI) meter over the utility's wireless mesh radio (Home Area Network).\n\nThis page is the entry point. Follow the operations in the sidebar for the full request/response contract, schema definitions, and error semantics.\n\n## Audience\n\nThis API is intended for utility backend engineers integrating their CIS and AMI head-end systems with EnergyGrid. It is **not** a public consumer API.\n\n## Authentication\n\nAll endpoints require an OAuth 2.0 Bearer token obtained from the utility's identity provider. Tokens are short-lived (typically one hour) and must be refreshed via the utility IDP — EnergyGrid does not issue tokens. Pass the token in the `Authorization` header as `Bearer <token>`. See the [Integration Guide — Configure authentication (OAuth)](/docs/integration-guide#step-2-configure-authentication-oauth) for the end-to-end exchange.\n\n## Base URL\n\nAll requests are made against the production base URL, scoped by API version:\n\n```\nhttps://api.energygrid.example/v1\n```\n\nA staging environment is available for integration testing — see the **Servers** dropdown on each operation page.\n\n## Conventions\n\n- **Content type.** Requests and responses use `application/json` unless otherwise noted.\n- **Identifiers.** Customer identifiers (`clientId`), Premise identifiers (`externalSiteId`), and Meter identifiers (`meterId`) are opaque strings owned by the utility CIS. EnergyGrid does not generate them.\n- **Account status.** The `AcctStatus` field uses the utility CIS encoding (`A` = Active, `C` = Closed, `S` = Suspended). EnergyGrid mirrors the value rather than translating it.\n- **Idempotency.** Customer sync (`POST /powerbox/customer`) is idempotent on `clientId` — repeating the call with the same payload is safe. Hardware binding (`POST /powerbox/bind`) is **not** idempotent; check `actionCode` and the meter's current bind state before retrying.\n- **Errors.** Errors return the relevant 4xx status code with no body. Reason codes are derived from the status: `400` invalid payload, `401` invalid or expired token, `403` account locked or closed, `404` unknown identifier.\n\n## Versioning\n\nThe API is versioned via the URL path (`/v1`). Breaking changes ship under a new major version with at least 90 days of overlap. Non-breaking additions (new optional fields, new enum values on extensible enums) ship within the current version.\n\n## Sample\n\nThis specification is published as a portfolio sample. The contracts, schemas, and conventions are representative of the kind of utility integration API that EnergyGrid (a fictional platform documented end-to-end on this site) would expose. The hostnames are illustrative and do not resolve.",
    "version": "1.0.0",
    "contact": {
      "name": "Leandro Gabriel — Technical Writer",
      "url": "https://www.linkedin.com/in/leandro-gabriel-8aab31167/",
      "email": "lgos99921@gmail.com"
    },
    "license": {
      "name": "MIT",
      "url": "https://opensource.org/licenses/MIT"
    }
  },
  "servers": [
    {
      "url": "https://api.energygrid.example/v1",
      "description": "Production — illustrative URL, does not resolve"
    },
    {
      "url": "https://staging.api.energygrid.example/v1",
      "description": "Staging — for integration testing against synthetic data"
    }
  ],
  "tags": [
    {
      "name": "Customer",
      "description": "Customer Information System (CIS) synchronisation. Push account profiles, service addresses (Premises), and meter configurations from the utility CIS into EnergyGrid. Calls are idempotent on `clientId`."
    },
    {
      "name": "Hardware",
      "description": "Bind and unbind EnergyGrid-supplied devices (DataBridge, SmartLink) to AMI meters over the utility's Home Area Network mesh radio. Binding is not idempotent — check the meter's current bind state before retrying."
    }
  ],
  "paths": {
    "/powerbox/customer": {
      "post": {
        "summary": "Customer Synchronization",
        "description": "Syncs customer account details, service addresses (Premises), and meter configurations from the utility CIS to EnergyGrid.",
        "operationId": "syncCustomer",
        "tags": ["Customer"],
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Customer data synchronized successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CustomerResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or expired token"
          },
          "403": {
            "description": "Forbidden - Account locked or closed"
          }
        }
      }
    },
    "/powerbox/bind": {
      "post": {
        "summary": "Hardware Binding",
        "description": "Links a DataBridge or SmartLink device to the AMI meter over the utility's wireless mesh radio (Home Area Network).",
        "operationId": "bindDevice",
        "tags": ["Hardware"],
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BindRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Bind request submitted successfully"
          },
          "400": {
            "description": "Invalid request payload"
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "description": "OAuth 2.0 Bearer token from the Utility IDP"
      }
    },
    "schemas": {
      "CustomerResponse": {
        "type": "object",
        "properties": {
          "clientId": {
            "type": "string",
            "description": "Unique customer identifier",
            "example": "123456789"
          },
          "firstName": {
            "type": "string",
            "example": "John"
          },
          "lastName": {
            "type": "string",
            "example": "Doe"
          },
          "AcctStatus": {
            "type": "string",
            "description": "Account status (A = Active)",
            "example": "A"
          },
          "customerSites": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CustomerSite"
            }
          }
        }
      },
      "CustomerSite": {
        "type": "object",
        "properties": {
          "externalSiteId": {
            "type": "string",
            "description": "Premise ID from utility CIS",
            "example": "5113305555"
          },
          "isPrimary": {
            "type": "boolean",
            "example": true
          },
          "address1": {
            "type": "string",
            "example": "123 Main St."
          },
          "city": {
            "type": "string",
            "example": "Detroit"
          },
          "state": {
            "type": "string",
            "example": "MI"
          },
          "postalCode": {
            "type": "string",
            "example": "48067"
          },
          "meters": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Meter"
            }
          }
        }
      },
      "Meter": {
        "type": "object",
        "properties": {
          "meterId": {
            "type": "string",
            "example": "123456789"
          },
          "meterType": {
            "type": "string",
            "enum": ["Electric", "Gas"],
            "example": "Electric"
          },
          "unitOfMeasure": {
            "type": "string",
            "example": "kWh"
          },
          "isAMI": {
            "type": "boolean",
            "description": "Indicates if meter supports Advanced Metering Infrastructure",
            "example": true
          },
          "rateCode": {
            "type": "string",
            "example": "RES1"
          },
          "billingCycleId": {
            "type": "integer",
            "example": 1
          }
        }
      },
      "BindRequest": {
        "type": "object",
        "required": ["hemProfileId", "macAddress", "installCode", "meterId", "actionCode"],
        "properties": {
          "hemProfileId": {
            "type": "string",
            "description": "Customer GUID",
            "example": "CustomerGuid"
          },
          "macAddress": {
            "type": "string",
            "description": "DataBridge MAC address",
            "example": "EB_MAC_Address"
          },
          "installCode": {
            "type": "string",
            "description": "DataBridge install code",
            "example": "EB_Install_Code"
          },
          "meterId": {
            "type": "string",
            "example": "123456789"
          },
          "actionCode": {
            "type": "integer",
            "description": "1 = Bind, 0 = Unbind",
            "enum": [0, 1],
            "example": 1
          }
        }
      }
    }
  }
}
